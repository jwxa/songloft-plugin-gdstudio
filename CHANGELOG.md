# Changelog

本文件记录 Songloft GDStudio 插件的重要版本变化。日期采用 `YYYY-MM-DD`。

## [0.2.9] - 2026-07-30

### Added

- 增加宿主能力探测与兼容提示，旧版 Songloft 缺少试听、下载进度或播放器导航桥接时不再暴露不可用操作。

### Changed

- 缺少后台下载任务 API 时自动回退到官方 `songs.download` 同步接口，保留原格式和 `music_path` 路径约束，但不展示实时字节进度。
- 宿主缺少临时试听接口时，在首次确认 404 后隐藏试听按钮，并引导用户先入库、再从 Songloft 主播放器播放。
- 客户端缺少 `host.openPlayer()` 时继续保留已入库歌曲，并提示用户手动返回主界面播放。

## [0.2.8] - 2026-07-29

### Added

- 提供可直接添加到 Songloft 插件商店的 `registry.json` 个人订阅源。
- 在公开 `plugin.json` 中提供 GitHub Release 下载地址，使插件支持从注册源一键安装和更新。

## [0.2.7] - 2026-07-29

### Fixed

- Android/iOS WebView 在音源解析完成后改为等待用户明确点击“播放试听”，避免异步解析导致用户手势失效并触发自动播放拦截。
- 桌面端自动播放被浏览器或 WebView 策略拒绝时保留试听会话，并优雅降级为手动播放按钮，而不是将其误报为音源解析失败。

## [0.2.6] - 2026-07-29

### Added

- 提供网易云、酷我和腾讯的轻量搜索、独立分页、单源失败隔离和一次自动重试。
- 提供完整歌曲纯流式试听、Range 拖动、会话释放和最高可用质量降级。
- 提供手动入库、同源重解析、按需元数据补全和主播放器播放入口。
- 提供单曲与批量 FIFO 下载队列、真实字节进度、1–3 并发配置和相对路径模板。
- 提供友好错误摘要与按需展示技术详情的弹层。
- 提供进入页面时必须勾选同意的个人研究使用须知。

### Changed

- 插件源码迁移到独立仓库 `jwxa/songloft-plugin-gdstudio`，Manifest 更新地址同步指向独立仓库。
- 腾讯来源改用 musicdl `QQMusicClient` 已收录的同源接口，不再向不支持腾讯的 GDStudio 公开端点发送请求。
- 无损音频显示格式与“无损”标签，不再把上游名义数值展示为固定精确码率。
- 下载保留上游现成格式，不执行额外转码。

### Security

- 试听真实 URL 与请求头仅保存在宿主临时会话中，不暴露给插件页面。
- 下载路径限制为 `music_path` 下的相对模板，并拒绝绝对路径和目录逃逸。
- 源码和发布包不包含账号、Token、Cookie、私有密钥或部署环境配置。

### Known Limitations

- 第三方上游接口可能超时、限流、改变格式或停止服务。
- 付费、会员和地区限制内容不保证能够解析或播放。
- 客户端全屏播放器跳转需要对应 Songloft 客户端导航桥接支持。
