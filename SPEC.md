# Songloft GDStudio 插件 V1 规格

## Problem Statement

Songloft 用户希望在不预先污染曲库的前提下搜索并试听 GDStudio 提供的在线音乐，确认内容和版本符合预期后，再明确选择添加到 Songloft 曲库或下载到本地音乐目录。

当前 Songloft 插件可以创建远程歌曲并通过 `/api/music/url` 动态解析播放地址，但主播放器只接受已入库歌曲 ID。现有插件 HTTP Handler 也不适合持续代理完整音频流和 Range 请求，因此仅靠当前插件 API 无法同时满足“搜索结果不入库”“完整歌曲试听”“可靠处理上游请求头”和“支持拖动播放”。

用户的部署场景是私有、非商业、小范围使用，最多同时 5 人。V1 必须保持普通 Songloft 插件形态，不引入 Python、musicdl CLI、额外容器或特殊 Python Docker 镜像。

## Solution

提供独立的 `songloft-plugin-gdstudio` 插件，并在 Songloft 后端增加一项与具体插件无关的临时试听能力。

插件使用 TypeScript 在 QuickJS 中直接调用上游公开接口，实现轻量搜索、音频地址解析、封面获取和歌词获取。网易云与酷我优先使用 GDStudio，腾讯使用 musicdl 2.13.4 `QQMusicClient` 已收录的同源接口；插件不安装或运行 Python 包，也不依赖网页签名或 Cloudflare Cookie。

搜索结果首先只存在于插件页面。用户点击试听时，插件通过经过 JWT 认证的接口创建临时试听会话；Songloft 后端解析插件返回的真实音源和请求头，生成短期随机 Token，并通过通用流式代理端点向插件页面的迷你播放器提供完整歌曲。试听不会创建歌曲记录，也不会写入临时音频文件。

用户满意后，可以手动添加单曲或批量添加到曲库。加入曲库的歌曲使用现有远程歌曲模型和 `/api/music/url` 动态解析链路。用户点击“下载到本地”时，该动作被视为明确授权：系统创建或复用远程歌曲记录，然后调用现有 `songs.download` 将最高可用质量的原始音频保存到 `music_path`，不执行转码，并写入可用的歌曲标签、歌词和封面。

## User Stories

1. 作为 Songloft 用户，我希望搜索 GDStudio 音乐，以便发现尚未加入曲库的歌曲。
2. 作为 Songloft 用户，我希望默认搜索 GDStudio 当前公开 API 可用的网易云和酷我来源，并明确看到腾讯来源的可用状态，以便不会重复触发已知无效请求。
3. 作为 Songloft 用户，我希望启用或停用单个子音源，以便控制搜索范围。
4. 作为 Songloft 用户，我希望“全部”搜索同时查询已启用来源，以便一次获得多个来源的候选结果。
5. 作为 Songloft 用户，我希望结果按可用来源分组，以便明确区分网易云和酷我的版本。
6. 作为 Songloft 用户，我希望每个来源拥有独立分页，以便只加载我关心的来源后续结果。
7. 作为 Songloft 用户，我希望首屏每个来源返回 10 条结果，以便兼顾速度和可浏览性。
8. 作为 Songloft 用户，我希望单个来源失败时仍看到其他来源结果，以便局部故障不阻断搜索。
9. 作为 Songloft 用户，我希望搜索阶段优先快速返回基础元数据，以便不必等待每首歌曲完成音频和歌词解析。
10. 作为 Songloft 用户，我希望搜索结果展示标题、歌手、专辑、时长和来源，以便判断候选歌曲。
11. 作为 Songloft 用户，我希望不同来源的同名歌曲保持独立，以便自行选择具体版本。
12. 作为 Songloft 用户，我希望点击试听时不创建曲库歌曲，以便试听行为不会污染曲库。
13. 作为 Songloft 用户，我希望试听完整歌曲，以便充分判断歌曲和版本是否符合预期。
14. 作为 Songloft 用户，我希望试听支持暂停和拖动，以便按需检查歌曲片段。
15. 作为 Songloft 用户，我希望试听从最高可用质量开始，以便获得最佳试听体验。
16. 作为 Songloft 用户，我希望最高质量不可用时自动逐档降级，以便仍能完成试听。
17. 作为 Songloft 用户，我希望试听只在插件页面的迷你播放器中运行，以便保持搜索结果与主曲库隔离。
18. 作为 Songloft 用户，我希望试听 URL 不暴露真实上游地址，以便减少临时音源泄露。
19. 作为 Songloft 用户，我希望试听会话自动过期，以便无效链接不会长期可用。
20. 作为 Songloft 用户，我希望停止试听后释放会话，以便避免占用有限的会话配额。
21. 作为 Songloft 用户，我希望明确点击“添加到曲库”后才创建远程歌曲，以便保持操作可控。
22. 作为 Songloft 用户，我希望批量选择搜索结果加入曲库，以便快速整理多首歌曲。
23. 作为 Songloft 用户，我希望批量添加逐项报告成功、已存在和失败，以便知道每首歌曲的结果。
24. 作为 Songloft 用户，我希望重复添加时复用已有歌曲，以便避免重复曲库记录。
25. 作为 Songloft 用户，我希望入库歌曲每次播放时重新解析当前最高可用音质，以便不依赖过期直链。
26. 作为 Songloft 用户，我希望原来源失效时只在同一来源内重新匹配，以便避免自动播放错误版本。
27. 作为 Songloft 用户，我希望跨来源替换必须手动确认，以便我能控制歌曲身份变化。
28. 作为 Songloft 用户，我希望点击“下载到本地”时自动完成入库或复用，以便不必重复操作。
29. 作为 Songloft 用户，我希望勾选多首歌曲后按顺序加入下载队列，并设置最大并发数，以便兼顾批量效率与带宽、磁盘负载。
30. 作为 Songloft 用户，我希望下载最高可用质量的现成音频，以便保留来源提供的最佳文件。
31. 作为 Songloft 用户，我希望下载过程不转码，以便避免额外 CPU 消耗和二次有损。
32. 作为 Songloft 用户，我希望下载后写入标题、歌手、专辑、歌词和封面，以便本地文件具有完整元数据。
33. 作为 Songloft 用户，我希望封面或歌词获取失败时仍能入库和下载，以便增强数据故障不阻断核心流程。
34. 作为 Songloft 用户，我希望下载根目录始终受 Songloft `music_path` 管理，以便文件仍位于受控音乐目录。
35. 作为 Songloft 用户，我希望配置相对下载路径模板，以便按歌手和专辑组织文件。
36. 作为 Songloft 用户，我希望路径模板被安全校验，以便不能写出 `music_path` 或覆盖任意文件。
37. 作为 Songloft 用户，我希望页面展示每项下载的排队、准备、字节进度、写入和完成状态，以便知道任务是否仍在推进。
38. 作为 Songloft 用户，我希望插件页面适配 Web、桌面和移动端，以便在不同客户端获得一致功能。
39. 作为移动端用户，我希望播放由明确点击触发，以便兼容 WebView 自动播放限制。
39. 作为管理员，我希望最多 5 人可同时使用插件，以便满足小范围私有部署需求。
40. 作为管理员，我希望搜索、解析和试听会话存在并发上限，以便插件不会耗尽服务资源。
41. 作为管理员，我希望插件直接访问 GDStudio 而不维护独立代理配置，以便减少配置复杂度。
42. 作为管理员，我希望试听音频不落盘，以便不会产生第二套临时缓存和清理负担。
43. 作为管理员，我希望服务重启后所有试听会话失效，以便临时能力凭证不会跨重启残留。
44. 作为管理员，我希望插件显示自身版本和参考的 musicdl/GDStudio 协议版本，以便排查兼容性问题。
45. 作为维护者，我希望 GDStudio 逻辑与 Songloft 通用试听代理解耦，以便其他插件未来可以复用试听能力。
46. 作为维护者，我希望核心试听 API 不包含任何 GDStudio 专属字段，以便保持模块职责单一。
47. 作为维护者，我希望插件使用稳定的来源和歌曲标识构造去重键，以便同来源歌曲不会重复创建。
48. 作为维护者，我希望错误能区分搜索、解析、试听、元数据、入库和下载阶段，以便快速定位故障。
49. 作为维护者，我希望敏感 Token、真实音源和认证请求头不进入普通日志，以便降低凭证泄露风险。
50. 作为维护者，我希望所有新增后端路由出现在 Swagger 中，以便 API 文档与代码保持同步。

## Implementation Decisions

- 插件作为独立项目 `songloft-plugin-gdstudio` 开发，不放入 Songloft 核心业务模块，也不把 GDStudio 协议实现写入 Go 后端。
- V1 使用纯 TypeScript 和 Songloft Plugin SDK，在 QuickJS 中运行；不使用 Python、musicdl CLI、外部二进制、额外容器或特殊运行镜像。
- 搜索、封面、歌词和音频地址解析优先使用 GDStudio 官方公开 API，音质降级和数据映射参考 musicdl 2.13.4。仅当 `kuwo` 来源在全部 GDStudio 音质档位均无可用地址时，才使用 musicsquare 采用的酷我同源解析接口，并保持原歌曲 ID 和来源不变。项目必须包含适用的来源说明、版权信息和非商业许可文本；V1 定位为私有、非商业使用。
- 插件默认启用 `netease` 和 `kuwo`，并将已启用来源及下载路径模板保存到插件持久化存储。`tencent` 保留为明确的不可用状态；GDStudio 当前公开 API 不接受该来源时不得发送请求，也不得自动替换为其他来源。
- 搜索采用轻量路径，只返回稳定歌曲身份和展示所需元数据。不得在首屏搜索中为全部结果同步下载歌词、测试所有音质或获取完整音频文件。
- 搜索结果按子音源分组，每个来源独立分页，默认每页 10 条。不同来源不做自动合并。
- 稳定去重键采用 `gdstudio:<root_source>:<identifier>`。不同子音源中的同名歌曲视为不同候选。
- `source_data` 保存稳定身份，包括子音源、歌曲标识及必要的匹配元数据；不得持久化容易过期的最终音频直链。
- 插件实现现有音源协议的搜索和 `/api/music/url` 解析能力。入库歌曲播放时，根据稳定身份重新请求当前最高可用音质。
- 音质请求档位固定为 `999 → 740 → 320 → 192 → 128`。试听和入库播放使用同一降级顺序；有损格式展示上游返回码率，无损格式只展示实际容器与“无损”标识，不把备用接口返回的 `2000` 等名义值表示为精确固定码率。
- 酷我同源备用解析直接返回上游现成音频文件及其实际格式、码率和必要请求头，不做转码；备用接口失败后才进入同来源重新匹配，且不得自动切换子音源。
- 同一来源内允许在地址失效时根据稳定 ID 或标题、歌手、时长重新匹配。禁止未经用户确认自动切换到其他子音源。
- Songloft 后端新增通用试听会话管理器。该模块只认识插件入口、opaque `source_data`、解析后的 URL、请求头、所有者、过期时间和会话状态，不认识 GDStudio 业务字段。
- 创建试听会话的 API 需要 Bearer JWT。请求指定插件入口和 opaque `source_data`；后端调用插件 `/api/music/url`，成功后返回随机 Token、流地址和过期时间。
- 试听流端点以高熵随机 Token 作为短期能力凭证，不要求播放器附加 JWT。Token 默认 30 分钟过期，只能访问创建时绑定的单一上游资源。
- 试听代理必须转发 Range 相关请求和响应语义，包括 `Range`、`If-Range`、`Accept-Ranges`、`Content-Range`、`Content-Length`、状态码 200/206，以及必要的 MIME 信息。
- 试听代理使用解析结果中的上游请求头，并执行现有的 URL/主机安全校验。客户端不能在会话创建后修改 URL 或上游请求头。
- 试听代理不将音频写入磁盘，不复用正式音乐缓存，也不创建临时歌曲数据库记录。服务重启时所有内存会话自然失效。
- 提供显式释放试听会话的能力；同时通过 TTL 后台清理保证异常关闭页面时不会永久占用资源。
- 并发按最多 5 人设计：全局同时搜索任务上限 2、音源解析上限 4、每用户有效试听会话上限 8、全局有效试听会话上限 32。超过限制时返回可识别的限流错误，而不是无限创建 goroutine 或请求。
- 插件页面包含搜索、来源筛选、独立分页、结果列表、批量选择、迷你播放器、添加到曲库、下载到本地、设置和错误状态。
- 迷你播放器只负责试听，不接入 Songloft 主播放队列，不保证离开插件页面后继续播放，也不提供后台系统媒体控制。
- 添加到曲库是显式操作。单曲添加先验证音源可解析，再补全可用元数据并调用现有远程歌曲创建能力；已知无可播放地址时返回 `422` 且不创建歌曲，增强元数据失败不阻断创建。
- 批量添加不是事务性全有或全无操作；必须逐项返回成功、已存在和失败状态。
- “下载到本地”是显式操作。下载前先验证音源可解析；若歌曲尚未入库则创建，若已存在则复用，随后通过 `songs.downloadStart` 提交后台任务，并用 `songs.downloadStatus` 轮询进度。已知无可播放地址时返回 `422` 且不创建或下载歌曲。
- 后台下载任务由宿主管理，返回高熵任务 ID、所有者隔离的状态快照和真实字节进度；插件 HTTP 请求只负责提交或查询，不等待完整文件下载，因此不得依赖放大全局 QuickJS 超时。
- 页面队列采用 FIFO，最大并发可配置为 1–3，默认 2；宿主后台任务模块另设全局并发 4，防止多个插件页面或用户共同打满下载链路。
- 下载调用不传转码格式或转码质量，确保保留上游现成音频格式。不可用音质通过 GDStudio 地址解析阶段降级，而不是通过 FFmpeg 转码。
- 下载默认开启元数据写入。标题、歌手、专辑、歌词和封面按已获得内容写入；单项写入失败只告警，不改变下载成功语义。
- 下载根目录固定为 Songloft `music_path`。插件只能持久化和传递相对路径模板，默认 `downloads/{artist}-{album}/{title}`，并复用 Songloft 现有模板校验、非法字符处理和重名文件避让能力。
- 插件不新增 HTTP Proxy 设置。QuickJS 的外发请求使用宿主当前 JSRuntime HTTP 行为直接访问 GDStudio。
- 后端新增路由必须遵守现有 REST 响应规范，错误返回统一的 `error` 和 `detail` 结构，并补全 Swagger 注释和生成产物。
- 不新增数据库迁移。试听会话完全驻留内存；正式歌曲继续使用现有歌曲表、插件入口、opaque `source_data` 和去重键。
- Songloft 核心与插件分别维护版本。插件设置或状态页显示插件版本、参考 musicdl 版本和 GDStudio 协议版本，便于人工升级验证。

## Testing Decisions

- 测试只断言外部可观察行为，不绑定内部函数名、goroutine 数量、缓存容器类型或具体 UI DOM 结构。
- 由于 Songloft 核心和独立插件是两个可独立发布的单元，V1 使用两个最高层测试接缝，而不是跨仓库共享内部实现。
- 核心测试接缝是 HTTP 黑盒试听会话 API。测试通过伪造插件解析器和本地上游音频服务器，覆盖创建会话、Token 播放、200/206 Range、请求头转发、过期、主动释放、无效 Token、上游失败、SSRF 拒绝和并发限制。
- 核心 API 测试沿用现有 Handler/Router 测试模式和真实 HTTP Recorder/测试服务器，不直接测试私有会话存储实现。
- 插件测试接缝是插件 HTTP 契约。测试使用可控的模拟 GDStudio 服务，从插件搜索端点进入，覆盖官方 API 查询、来源筛选、分页、轻量 DTO、最高音质降级、元数据补全和 `/api/music/url` 输出。
- 插件测试不访问真实 GDStudio 网络服务，以避免上游波动、限流和地区网络差异造成不稳定测试。
- 官方 API 客户端测试必须确认使用 GET 查询参数且不附加网页签名字段。
- 为稳定身份和去重键增加契约测试，确保相同来源与 ID 稳定复用，不同来源不会误合并。
- 为批量添加增加行为测试，验证成功、已存在和失败可以同时返回，单项失败不回滚成功项。
- 为下载动作增加宿主桥接测试，验证任务 ID 所有者隔离、FIFO、全局并发上限、真实字节进度和完成/失败状态。
- 为插件页面增加队列测试，验证默认并发 2 时第三项必须等待，并锁住宿主 `queued` 状态不能导致重复提交的竞态。
- 为插件设置增加持久化测试，验证来源开关、下载路径模板和 1–3 最大并发可保存、恢复，并拒绝无效值。
- 为响应式页面执行 Web、窄屏移动视口和桌面 WebView 的基本交互测试，重点验证用户手势触发播放、迷你播放器状态和操作按钮可达性。
- 验收测试至少覆盖 Web、Android WebView 和 Windows WebView；其他平台共享同一 Web UI，但不得宣称经过完整平台专项验证。
- 后端变更完成后必须运行格式化、静态检查、相关单元测试、完整 Go 测试和 Swagger 生成校验。
- 插件变更完成后必须运行 TypeScript 类型检查、插件构建、Manifest 校验和契约测试。

## Out of Scope

- 安装或运行完整 musicdl Python 包。
- Python CLI、PyInstaller、内置 Python 运行时或常驻后台进程。
- 自定义 Python Docker 镜像或额外 sidecar 容器。
- 除本规格明确采用的腾讯 `QQMusicClient` 同源接口之外的其他 musicdl 客户端。
- 默认启用网易云、酷我、腾讯之外的其他音源。
- 搜索结果跨来源智能合并。
- 未经用户确认的跨来源自动替换。
- 搜索结果直接加入 Songloft 主播放队列。
- 试听后台播放、系统媒体控制和离开插件页面后的持续播放保证。
- 试听音频落盘、断点缓存或复用正式 CacheService。
- 暂停、恢复或取消正在运行的下载任务。
- FFmpeg 转码、格式选择和码率转换。
- `music_path` 之外的下载目标目录。
- 新增全局下载路径设置或修改现有 `music_path` 配置语义。
- 新增数据库表或持久化试听会话。
- 自动更新参考 musicdl/GDStudio 协议实现。
- 对公共插件市场发布、商业使用或许可证重新授权。

## Further Notes

- 本规格面向私有、非商业部署。若未来公开发布，必须重新审查 musicdl 许可、GDStudio 服务条款、来源声明和再分发边界。
- GDStudio 是外部服务，Songloft 无法保证其长期稳定性、内容可用性或 API 兼容性。插件必须将上游错误清晰呈现，并允许单个来源独立失败。
- 纯 TypeScript 方案取消了此前讨论的 Python CLI、完整 musicdl 安装和特殊镜像方案；这些决定不再属于 V1。
- 通用试听代理是唯一需要进入 Songloft 核心的新能力。GDStudio 官方 API 调用、分页、元数据和音质策略全部留在独立插件中。
- 实施应优先完成后端试听契约和插件模拟服务契约，再接入真实 GDStudio，避免在不可控上游上调试基础架构。
